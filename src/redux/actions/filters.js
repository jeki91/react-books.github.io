export const setSortBy = (name) => ({
	type: 'SET_SORT_BY',
	payload: name,
});

export const setCategory = (name) => ({
	type: 'SET_CATEGORY',
	payload: name,
});

export const setTab = (name) => ({
	type: 'SET_TAB',
	payload: name,
});